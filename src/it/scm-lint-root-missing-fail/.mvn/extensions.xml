<?xml version="1.0" encoding="UTF-8"?>
<extensions>
    <extension>
        <groupId>network.ike.tooling</groupId>
        <artifactId>ike-version-management-extension</artifactId>
        <!-- Released version; tracks the pin in ike-platform's pom.xml
             (network.ike.tooling__GA__ike-version-management-extension__VERSION).
             maven-invoker-plugin does not filter this file, so the
             literal is maintained by hand. -->
        <version>11</version>
    </extension>
</extensions>
